import { InputBoxFocusDirective } from './input-box-focus.directive';

describe('InputBoxFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBoxFocusDirective();
    expect(directive).toBeTruthy();
  });
});
