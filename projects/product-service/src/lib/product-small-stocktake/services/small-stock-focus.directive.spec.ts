import { SmallStockFocusDirective } from './small-stock-focus.directive';

describe('SmallStockFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SmallStockFocusDirective();
    expect(directive).toBeTruthy();
  });
});
