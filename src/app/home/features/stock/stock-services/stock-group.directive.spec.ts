import { StockGroupDirective } from './stock-group.directive';

describe('StockGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new StockGroupDirective();
    expect(directive).toBeTruthy();
  });
});
