import { MyInputDirective } from './my-input.directive';

describe('MyInputDirective', () => {
  it('should create an instance', () => {
    // const directive = new MyInputDirective();
    // expect(directive).toBeTruthy();
  });
});
